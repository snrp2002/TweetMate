import classes from "./Auth.module.css";
import AuthForm from "./AuthForm";
const Auth = () => {
  
  return (
    <div className={classes.auth}>
      <AuthForm/>
    </div>
  );
};
export default Auth;
